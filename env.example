# Environment configuration for the margin dashboard
# Copy this file to .env.local and modify as needed

# Data source type: 'api' or 'static'
VITE_DATA_SOURCE=api

# API configuration (used when VITE_DATA_SOURCE=api)
VITE_API_URL=http://localhost:9008

# Static data path (used when VITE_DATA_SOURCE=static)
VITE_STATIC_DATA_PATH=/data/dashboard-data.json

# Refresh interval in milliseconds (0 to disable auto-refresh)
VITE_REFRESH_INTERVAL=30000

# For production/static hosting, set:
# VITE_DATA_SOURCE=static
# VITE_REFRESH_INTERVAL=0
